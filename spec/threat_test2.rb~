def loop_thread(sec)
  p "hello"
  Thread.start(sec) do |wait_time|
    sleep wait_time
    puts "\a Time up. Type return-key to finish.."
  end
end

def time_training(period)
  loop_thread(period)
  p "helo2"
end

time_training(3)

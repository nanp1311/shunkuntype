create        #[動]無から生成する_IT業界で最も使われる英単語の一つ、なにかを新しく作成するときに使われることが多い
get           #[動]手に入れる__memo
find          #[動]見つける、データを取得する_memo
listen        #[動]イベントを監視する_memo
put           #[動]置く、セットする_memo
is            #[動]～である、～を継承している_memo
have          #[動]～を構成要素として持つ、has_memo
add           #[動]付け加える_memo
delete        #[動]削除する,del_memo
success       #[名]成功_memo
error	      #[名]致命的な欠陥_memo
push	      #[名]最後尾へ追加する_memo
pull	      #[動]引っ張る_memo
return	      #[動]戻る_memo
default	      #[名]初期状態_memo
interface     #[名]境界面、約束事_memo
commit	      #[動]引っ込みがつかない約束をする、確約する_memo
contain	      #[動]包み込む、包含する、含める_memo
include	      #[動]含む、取り込む_memo
length	      #[名]文字列の数や配列の個数_memo
size	      #[名]データの大きさ、配列などの個数_memo
range	      #[名]幅、守備範囲_memo
join	      #[動]参加する、結合する_memo
union	      #[名]合体、和、和集合_memo
merge	      #[動]ひとつにする_memo
concat	      #[動]文字列を連結する_memo
append	      #[動]文章の最後に追加する_memo
relation      #[名]関係、テーブル間のつながり、rel_memo
association   #[名]つながり、協会_memo
evidence      #[名]キャプチャ、根拠_memo
pending	      #[名]保留_memo
domein	      #[名]領域_memo
generate      #[動]時間と労力をかけて生み出す、gen_memo
fetch	      #[動]行って取ってくる_memo
factory	      #[名]インスタンスを生成する工場_memo
construct     #[動]オブジェクトを新しく生成する_memo
compose	      #[動]構成する_memo
skip	      #[動]処理を飛ばす_memo
continue      #[動]処理を続行する_memo
next	      #[動]次の繰り返しに移る、右矢印_memo
development   #[名]開発、dev_memo
progress      #[動]進行する_memo
complete      #[動]正常に完了する_memo
launch	      #[動]アプリを起動する、サービスを開始する_memo
implement     #[動]実装する_memo
sanitise      #[動]解毒する、無害化する_memo
blank	      #[形]文字が書かれていない、空欄の_memo
remove	      #[動]取り除く、削除する_memo
quit	      #[動]やめる、アプリを終了する_memo
exit	      #[動](プログラムから)脱出する_memo
conflict      #[名]修正と修正の衝突_memo
fault	      #[名]欠陥_memo
close	      #[動]接続を切る、資源を解放する_memo
stream	      #[名]データの流れ_memo
import	      #[動]ほかのファイルを取り込む_memo
migrate	      #[動]移住する、移植する_memo
binary	      #[名]2進法、機械語_memo
octal	      #[名]8進法_memo
decimal	      #[名]10進法_memo
hexadecimal   #[名]16進法_memo
format	      #[名]型、ファイル形式_memo
store	      #[動]データを蓄える、貯蓄_memo
asset	      #[名]資産_memo
dump	      #[動]データを出力する_memo
result	      #[名]計算結果、結果セット_memo
row	      #[名]行_memo
column	      #[名]列、col_memo
archive	      #[動]圧縮して保存する_memo
attribute     #[名]所有者が推測できる属性_memo
analyze	      #[動]分析する_memo
alter	      #[動]スキーマに変更を加える_memo
convert	      #[動]ファイルの形式やサイズを変える、conv_memo
parse	      #[動]構文解析する、変換する_memo
populate      #[動]データで埋める_memo
cascade	      #[名]連鎖反応_memo
pluck	      #[動]摘み取る_memo
mutation      #[名]変異_memo
string	      #[名]文字列、ひとつなぎの文字_memo
char	      #[名]文字、固定長文字列_memo
varchar	      #[名]可変長文字列_memo
integer	      #[名]整数、int_memo
float	      #[名]浮動小数点数_memo
set	      #[名]重複を許さない集合_memo
array	      #[名]配列_memo
hash	      #[動]切り刻む_memo
map	      #[動]対応づける_memo
function      #[名]関数、func_memo
define	      #[動]定義する、def_memo
strict	      #[形]厳格モード_memo
local	      #[形]地方の、閉じた空間の_memo
global	      #[形]誰からでも見える_memo
scope	      #[名]範囲、スコープ_memo
refer	      #[動]参照する_memo
annotate      #[動]注釈を付ける_memo
schema	      #[名]スキーマ、概要_memo
environment   #[名]環境、env_memo
status	      #[名]時間で変化する状況_memo
initialize    #[動]初期化する、init_memo
layout	      #[名]配置、外側の共通部分_memo
offset	      #[名]ファイル内の現在位置_memo
locale	      #[名]各地域用の設定_memo
feature	      #[名]アプリの新しい目玉機能_memo
trait	      #[名]特徴、性格_memo
context	      #[名]背景、文脈、コンテキスト_memo
current	      #[形]いま対象としている、いま乗っている文脈上の_memo
async	      #[形]非同期の、個々が独立している_memo
temporary     #[形]一時的な、tmp_memo
state	      #[名]状態_memo
property      #[名]所有物、プロパティ、prop_memo
finally	      #[副]実行を保証する_memo
entry	      #[名]プログラムの起点、見出し、登録事項_memo
allow	      #[動]入場を許可する_memo
permit	      #[動]実行を許可する_memo
authorization #[名]パワー(権威)の授与、認可、auth_memo
private	      #[動]私的な、直接見られない_memo
public	      #[形]公の窓口の_memo
meta	      #[形]二次元の、高次元の_memo
require	      #[動]必要とする_memo
depend	      #[動]依存する_memo
request	      #[名]要求、リクエスト_memo
invalid	      #[形](法的に)受け入れられない_memo
flush	      #[動]バッファないのデータを掃き出す_memo
dispatch      #[動]送る_memo
render	      #[動]送り返す_memo
submit	      #[動]提出する、送信する_memo
yield	      #[動]制御を譲る_memo
assign	      #[動]あてがう、代入する_memo
alert	      #[名]異常の通知_memo
exception     #[名]例外_memo
assert	      #[動]主張する_memo
send	      #[動]送る_memo
path	      #[名]コマンドへ通じる道、ファイルの存在場所_memo
node	      #[名]節点、ネットワーク機器_memo
ignore	      #[動]無視する、除外する_memo
except	      #[動]除外する、ブラックリスト_memo
unique	      #[形]ユニーク、一意の、uniq_memo
filter	      #[名]フィルタ_memo
trim	      #[動]端の空白を除去する_memo
select	      #[動]選択する_memo
limit	      #[名]リミット、限度_memo
match	      #[動]一致する、マッチする_memo
process	      #[名]プロセス、proc_memo
task	      #[名]処理の単位、割り当てられた務め_memo
service	      #[名]サービス_memo
execute	      #[動]実行する、exe_memo
kill	      #[動]プロセスを止める_memo
shell	      #[名]シェル_memo
warning	      #[名]警告_memo
abort	      #[動]中断する_memo
failure	      #[名]失敗_memo
variable      #[名]変数、var_memo
load	      #[動]メモリに読み込む、負荷をかける_memo
invoke	      #[動]呼び出す_memo
interrupt     #[名]割り込む_memo
exist	      #[動]存在する_memo
compress      #[動]圧縮する_memo
identify      #[動]認識する、ファイルが見つかる_memo
deprecated    #[形]非推奨の、廃止予定の_memo
halt	      #[動]コンピュータを停止する_memo
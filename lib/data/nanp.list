create        #[動]無から生成する
get           #[動]手に入れる
find          #[動]見つける、データを取得する
listen        #[動]イベントを監視する
put           #[動]置く、セットする
is            #[動]～である、～を継承している
have          #[動]～を構成要素として持つ、has
add           #[動]付け加える
delete        #[動]削除する,del
success       #[名]成功
error	      #[名]致命的な欠陥
push	      #[名]最後尾へ追加する
pull	      #[動]引っ張る
return	      #[動]戻る
default	      #[名]初期状態
interface     #[名]境界面、約束事
commit	      #[動]引っ込みがつかない約束をする、確約する
contain	      #[動]包み込む、包含する、含む
include	      #[動]含む、取り込む
length	      #[名]文字列の数や配列の個数
size	      #[名]データの大きさ、配列などの個数
range	      #[名]幅、守備範囲
join	      #[動]参加する、結合する
union	      #[名]合体、和、和集合
merge	      #[動]ひとつにする
concat	      #[動]文字列を連結する
append	      #[動]文章の最後に追加する
relation      #[名]関係、テーブル間のつながり、rel
association   #[名]つながり、協会
evidence      #[名]キャプチャ、根拠
pending	      #[名]保留
domein	      #[名]領域
generate      #[動]時間と労力をかけて生み出す、gen
fetch	      #[動]行って取ってくる
factory	      #[名]インスタンスを生成する工場
construct     #[動]オブジェクトを新しく生成する
compose	      #[動]構成する
skip	      #[動]処理を飛ばす
continue      #[動]処理を続行する
next	      #[動]次の繰り返しに移る、右矢印
development   #[名]開発、dev
progress      #[動]進行する
complete      #[動]正常に完了する
launch	      #[動]アプリを起動する、サービスを開始する
implement     #[動]実装する
sanitise      #[動]解毒する、無害化する
blank	      #[形]文字が書かれていない、空欄の
remove	      #[動]取り除く、削除する
quit	      #[動]やめる、アプリを終了する
exit	      #[動](プログラムから)脱出する
conflict      #[名]修正と修正の衝突
fault	      #[名]欠陥
close	      #[動]接続を切る、資源を解放する
stream	      #[名]データの流れ
import	      #[動]ほかのファイルを取り込む
migrate	      #[動]移住する、移植する
binary	      #[名]2進法、機械語
octal	      #[名]8進法
decimal	      #[名]10進法
hexadecimal   #[名]16進法
format	      #[名]型、ファイル形式
store	      #[動]データを蓄える、貯蓄
asset	      #[名]資産
dump	      #[動]データを出力する
result	      #[名]計算結果、結果セット
row	      #[名]行
column	      #[名]列、col
archive	      #[動]圧縮して保存する
attribute     #[名]所有者が推測できる属性
analyze	      #[動]分析する
alter	      #[動]スキーマに変更を加える
convert	      #[動]ファイルの形式やサイズを変える、conv
parse	      #[動]構文解析する、変換する
populate      #[動]データで埋める
cascade	      #[名]連鎖反応
pluck	      #[動]摘み取る
mutation      #[名]変異
string	      #[名]文字列、ひとつなぎの文字
char	      #[名]文字、固定長文字列
varchar	      #[名]可変長文字列
integer	      #[名]整数、int
float	      #[名]浮動小数点数
set	      #[名]重複を許さない集合
array	      #[名]配列
hash	      #[動]切り刻む
map	      #[動]対応づける
function      #[名]関数、func
define	      #[動]定義する、def
strict	      #[形]厳格モード
local	      #[形]地方の、閉じた空間の
global	      #[形]誰からでも見える
scope	      #[名]範囲、スコープ
refer	      #[動]参照する
annotate      #[動]注釈を付ける
schema	      #[名]スキーマ、概要
environment   #[名]環境、env
status	      #[名]時間で変化する状況
initialize    #[動]初期化する、init
layout	      #[名]配置、外側の共通部分
offset	      #[名]ファイル内の現在位置
locale	      #[名]各地域用の設定
feature	      #[名]アプリの新しい目玉機能
trait	      #[名]特徴、性格
context	      #[名]背景、文脈、コンテキスト
current	      #[形]いま対象としている、いま乗っている文脈上の
async	      #[形]非同期の、個々が独立している
temporary     #[形]一時的な、tmp
state	      #[名]状態
property      #[名]所有物、プロパティ、prop
finally	      #[副]実行を保証する
entry	      #[名]プログラムの起点、見出し、登録事項
allow	      #[動]入場を許可する
permit	      #[動]実行を許可する
authorization #[名]パワー(権威)の授与、認可、auth
private	      #[動]私的な、直接見られない
public	      #[形]公の窓口の
meta	      #[形]二次元の、高次元の
require	      #[動]必要とする
depend	      #[動]依存する
request	      #[名]要求、リクエスト
invalid	      #[形](法的に)受け入れられない
flush	      #[動]バッファないのデータを掃き出す
dispatch      #[動]送る
render	      #[動]送り返す
submit	      #[動]提出する、送信する
yield	      #[動]制御を譲る
assign	      #[動]あてがう、代入する
alert	      #[名]異常の通知
exception     #[名]例外
assert	      #[動]主張する
send	      #[動]送る
path	      #[名]コマンドへ通じる道、ファイルの存在場所
node	      #[名]節点、ネットワーク機器
ignore	      #[動]無視する、除外する
except	      #[動]除外する、ブラックリスト
unique	      #[形]ユニーク、一意の、uniq
filter	      #[名]フィルタ
trim	      #[動]端の空白を除去する
select	      #[動]選択する
limit	      #[名]リミット、限度
match	      #[動]一致する、マッチする
process	      #[名]プロセス、proc
task	      #[名]処理の単位、割り当てられた務め
service	      #[名]サービス
execute	      #[動]実行する、exe
kill	      #[動]プロセスを止める
shell	      #[名]シェル
warning	      #[名]警告
abort	      #[動]中断する
failure	      #[名]失敗
variable      #[名]変数、var
load	      #[動]メモリに読み込む、負荷をかける
invoke	      #[動]呼び出す
interrupt     #[名]割り込む
exist	      #[動]存在する
compress      #[動]圧縮する
identify      #[動]認識する、ファイルが見つかる
deprecated    #[形]非推奨の、廃止予定の
halt	      #[動]コンピュータを停止する
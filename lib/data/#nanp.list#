create        #[動]無から生成する
get           #[動]手に入れる
find          #[動]見つける、データを取得する
listen        #[動]イベントを監視する
put           #[動]置く、セットする
is            #[動]～である、～を継承している
have          #[動]～を構成要素として持つ
has           #[動]～を構成要素として持つ
add           #[動]付け加える
del           #[動]削除する
delete        #[動]削除する
success       #[名]成功
error	      #[名]致命的な欠陥
push	      #[名]最後尾へ追加する
pull	      #[動]引っ張る
return	      #[動]戻る
default	      #[名]初期状態
interface     #[名]境界面、約束事
commit	      #[動]引っ込みがつかない約束をする、確約する　...Chapter1_end
contain	      #[動]包み込む
